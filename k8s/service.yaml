apiVersion: v1
kind: Service
metadata:
  name: smart-event-portal-service
spec:
  type: NodePort
  selector:
    app: smart-event-portal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30005
